const SOURCE_SYSTEM = 'ffc-ahwr'

const MESSAGE_RESULT_MAP = {
  unknown: 'UNKNOWN',
  unsent: 'UNSENT',
  sent: 'SENT',
  failed: 'FAILED'
}

module.exports = {
  SOURCE_SYSTEM,
  MESSAGE_RESULT_MAP
}
